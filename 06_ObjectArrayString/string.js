// let firstName = "mozahid"
// let middleName = '------'
// let lastName = `Izhar`

// let fullname = ` ${firstName} ${middleName} ${lastName}`
// /*
//  ``     - template literal 
//  method - string interpoletion*/
// console.log(fullname);


// ======================================
/**
 * Properties:
 
str.length tells how many characters are in a string. (length of string)

Methods:

str.toUpperCase () convert each letter into uppercase

str.toLowerCase () convert each letter into lowercase

str.trim () remove whitespaces

str.concat (str2) joins str2 with str1

str.includes () checks if string contains given piece of string

str.indexOf () gives the position (index) of the first match

str.charAt () gives the character at given index

str.replace (old, new) replaces first matched part of the string with something else

str.replaceAll (old, new) replaces all matched parts of the string with something else

str.slice (start, end) cuts out a piece of the string

str.split () the .split() method is used to break a string into parts and turn it into an array.
 */


// Escape Sequence Character = \n

// let str = new String("Mozahid")
// let str2 = " Izhar"

// console.log(str);

// console.log(str.length); // tells how many characters are in a string. (length of string)

// console.log(str.toUpperCase ()); //convert each letter into uppercase
// console.log(str.toLowerCase ()); //convert each letter into lowercase
// console.log(str.trim ()); // remove whitespaces

// console.log(str.concat (str2)); //str.concat (str2) joins str2 with str1

// console.log(str.includes ("M"));  // checks if string contains given piece of string

// console.log(str.indexOf("i")); // gives the position (index) of the first match

// console.log(str.replace ("M", "S")); //replaces first matched part of the string with something else


// =============================================